# Level 1 — Fundamentals

### 1. Even Number Check

Write:

```python
def is_even(n):
```

Return `True` if even, otherwise `False`.

📘 Docs: [https://docs.python.org/3/tutorial/controlflow.html#if-statements](https://docs.python.org/3/tutorial/controlflow.html#if-statements)

---

### 2. Compare Two Numbers

Write:

```python
def compare(a, b):
```

Return:

* `"greater"` if a > b
* `"equal"` if equal
* `"smaller"` otherwise

---

### 3. Sum of 1 to 100

Use a loop to compute the sum of numbers 1 to 100.

📘 Docs: [https://docs.python.org/3/tutorial/controlflow.html#for-statements](https://docs.python.org/3/tutorial/controlflow.html#for-statements)

---

### 4. List Basics

Given:

```python
numbers = [3, 7, 2, 9, 10]
```

Print:

* first element
* last element
* length of list

📘 Docs: [https://docs.python.org/3/tutorial/introduction.html#lists](https://docs.python.org/3/tutorial/introduction.html#lists)

---

### 5. Unique Values with Sets

Given:

```python
items = [1, 2, 2, 3, 4, 4, 5]
```

Convert to a set and print unique values.

📘 Docs: [https://docs.python.org/3/tutorial/datastructures.html#sets](https://docs.python.org/3/tutorial/datastructures.html#sets)

---

### 6. Tuple Unpacking

Given:

```python
person = ("Alice", 25, "Stockholm")
```

Unpack and print a sentence using all values.

📘 Docs: [https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences](https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences)

---

### 7. Grade Calculator

Write:

```python
def grade(score):
```

Rules:

* 90+ = A
* 80–89 = B
* 70–79 = C
* <70 = F

---

### 8. Describe a Person

Write:

```python
def describe_person(name, age=18, city="Unknown"):
```

Return a formatted description string.

📘 Docs: [https://docs.python.org/3/tutorial/controlflow.html#default-argument-values](https://docs.python.org/3/tutorial/controlflow.html#default-argument-values)

---

# Level 2 — Functions & Data Structures

### 9. Sum of List (No Built-in)

Write:

```python
def sum_list(nums):
```

Return the sum without using `sum()`.

---

### 10. Count Words

Write:

```python
def count_words(text):
```

Return number of words in a string.

📘 Docs: [https://docs.python.org/3/library/stdtypes.html#str.split](https://docs.python.org/3/library/stdtypes.html#str.split)

---

### 11. Dictionary Lookup

Write:

```python
def get_age(data, name):
```

Where `data` is:

```python
{"Alice": 25, "Bob": 30}
```

Return the age or `"Not found"`.

---

### 12. Dictionary Analysis

Write:

```python
def analyse_dict(dictionary):
```

It should print the [type](https://docs.python.org/3/library/functions.html#type) of each value (along with its length if applicable), for each key.

👉 Output example:

```
'Age' is a integer
'Name' is a string of size 9
```

---

### 13. Process Numbers Summary

Write:

```python
def process_numbers(nums):
```

Return a dictionary with:

* len
* sum
* min
* max
* average

---

### 14. Factorial Function

Write:

```python
def factorial(n):
```

Return `n!` (n factorial).

📘 Docs: [https://docs.python.org/3/tutorial/controlflow.html#defining-functions](https://docs.python.org/3/tutorial/controlflow.html#defining-functions)

---

### 15. Greatest Common Divisor (GCD)

Write:

```python
def gcd(a, b):
```

Return the greatest common divisor using the Euclidean algorithm.

---

# Level 3 — File Handling, CLI, and Real Data

### 16. File Copy with Line Numbers

Write a script that:

* Creates a copy of a text file
* Adds line numbers at the beginning of each line

📘 Docs: [https://docs.python.org/3/tutorial/inputoutput.html#reading-and-writing-files](https://docs.python.org/3/tutorial/inputoutput.html#reading-and-writing-files)

---

### 17. File Type Counter

Use `os.listdir()` to:

* Count how many files there are of each extension/type

📘 Docs: [https://docs.python.org/3/library/os.html#os.listdir](https://docs.python.org/3/library/os.html#os.listdir)

---

### 18. Word Frequency Analyzer

Write a script that:

* Reads a text file `text.txt`
* Counts frequency of each word
* Prints top 3 most common words
* Prints top 3 least common words

---

### 19. Command-Line Number Processor

Write a program that:

* Reads numbers from `sys.argv`
* Converts them to integers (handle errors)
* Prints:

  * sorted list
  * sum
  * number of even values

📘 Docs:

* [https://docs.python.org/3/library/sys.html#sys.argv](https://docs.python.org/3/library/sys.html#sys.argv)
* [https://docs.python.org/3/tutorial/errors.html](https://docs.python.org/3/tutorial/errors.html)

---

### 20. FASTA File Parser

Write a script that:

* Parses a FASTA file
* Stores sequences in a dictionary:

```python
{
    "sequence_id": "ACTG..."
}
```

👉 FASTA format example:

```
>seq1
ACTG
>seq2
TTAA
```

---

### 21. Protein Sequence Analysis

Write:

```python
def analyze_protein(sequence):
```

Return a dictionary with:

* length of sequence
* number of charged amino acids (`D, E, K, R, H`)
* number of non-canonical amino acids (anything not in standard 20 AA)

📘 Reference: [https://en.wikipedia.org/wiki/Amino_acid](https://en.wikipedia.org/wiki/Amino_acid)


